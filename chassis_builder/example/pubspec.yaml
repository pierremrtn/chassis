name: example
description: Example app composing the example_auth chassis module.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.6.2

resolution: workspace

dependencies:
  chassis:
    path: ../../chassis
  example_auth:
    path: ../example_auth

dev_dependencies:
  chassis_builder:
    path: ../
  build_runner: ^2.15.0
