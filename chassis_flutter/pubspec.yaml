name: chassis_flutter
description: Flutter integration of the chassis framework — ViewModel, ViewModelProvider, AsyncBuilder, and event handling.
version: 1.0.0
repository: https://github.com/pierremrtn/chassis
homepage: https://affordant.gitbook.io/chassis/
topics: 
  - chassis
  - mvvm
  - cqrs
  - architecture
  - state-management

environment:
  sdk: ^3.6.2

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  chassis: ^1.0.0
  provider: ^6.1.5
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
